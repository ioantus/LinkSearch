<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Search external links by domain</title>
    <style type="text/css">
        .edit {
            border:1px solid #9E9E9E;
            color: #000000;
            padding: 12px 30px;
            margin-top: 2px;
            margin-bottom: 2px;
            font-size: 20px;;
            font-family: 'Roboto Condensed', Тahoma, sans-serif;
            background: #ccffff;
        }
        .button10 {
            display: inline-flex;
            margin: 10px;
            text-decoration: none;
            position: relative;
            font-size: 20px;
            line-height: 20px;
            padding: 12px 30px;
            color: #FFF;
            font-weight: bold;
            text-transform: uppercase;
            font-family: 'Roboto Condensed', Тahoma, sans-serif;
            background: #337AB7;
            cursor: pointer;
            border: 2px solid #BFE2FF;
        }
        .button10:hover,
        .button10:active,
        .button10:focus {
            color: #FFF;
        }
        .button10:after,
        .button10:before {
            position: absolute;
            height: 4px;
            left: 50%;
            background: #337AB7;
            bottom: -6px;
            content: "";
            transition: all 280ms ease-in-out;
            width: 0;
        }
        .button10:before {
            top: -6px;
        }
        .button10:hover:after,
        .button10:hover:before,
        .button10:active:after,
        .button10:active:before,
        .button10:focus:after,
        .button10:focus:before {
            width: 100%;
            left: 0;
        }
    </style>
</head>
<body>
<form name="search" action="run" method="get">
    <input class="edit" type="text" name="domain" placeholder="Enter domain">
    <input class="edit" type="text" name="maxLevel" placeholder="Maximum level">
    <button class="button10" type="submit">START</button>
</form>
</body>
</html>